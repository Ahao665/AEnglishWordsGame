# ✨ 魔法拼写 · Gesture Spelling Game

用**手势识别**玩英语拼写：摄像头 + MediaPipe 手部追踪，捏合拖拽字母、确认拼写、下一题，支持英语四级词汇随机出题与读音。

![React](https://img.shields.io/badge/React-18-61dafb?logo=react)
![Vite](https://img.shields.io/badge/Vite-5-646cff?logo=vite)
![MediaPipe](https://img.shields.io/badge/MediaPipe-Hands-4285f4?logo=google)

---

## 📤 上传到 GitHub 并让别人在线玩

1. **在 GitHub 新建仓库**（不要勾选 “Add a README”），记下仓库地址。
2. **本地初始化并推送**（若尚未初始化 git）：
   ```bash
   git init
   git add .
   git commit -m "feat: 魔法拼写游戏"
   git branch -M main
   git remote add origin https://github.com/<你的用户名>/<仓库名>.git
   git push -u origin main
   ```
3. **开启 GitHub Pages**：仓库 **Settings** → **Pages** → **Source** 选 **GitHub Actions**。
4. 推送后等待 **Actions** 里 “Deploy to GitHub Pages” 完成，别人即可访问 `https://<你的用户名>.github.io/<仓库名>/` 在线玩。

---

## 🎮 在线玩

若已开启 **GitHub Pages**，可直接在浏览器里玩（需摄像头和 HTTPS）：

1. 打开仓库 → **Settings** → **Pages**
2. **Build and deployment** → **Source** 选 **GitHub Actions**
3. 推送代码到 `main`（或 `master`）后，等待 Actions 里 “Deploy to GitHub Pages” 跑完
4. 访问：`https://<你的用户名>.github.io/<仓库名>/`

例如仓库名为 `gesture-spelling-game` 时：`https://octocat.github.io/gesture-spelling-game/`

---

## 🛠 本地运行

### 环境

- Node.js 18+
- 摄像头（用于手势识别）

### 步骤

```bash
# 克隆仓库
git clone https://github.com/<你的用户名>/<仓库名>.git
cd <仓库名>

# 安装依赖
npm install

# 启动开发服务器（默认 http://localhost:5173）
npm run dev
```

浏览器打开提示的地址，**允许摄像头权限** 即可开始玩。

### 构建与预览

```bash
# 构建生产版本
npm run build

# 本地预览构建结果
npm run preview
```

---

## 📖 玩法说明

1. **选择模式**：单手 / 双手
2. **举起手**：摄像头识别到手掌后自动开始一题
3. **拼写**：捏合手指“抓住”字母块，拖到上方槽位；松手放入
4. **取回**：拼错或想重排时，在已放字母的槽上**捏合**可取出该字母
5. **确认**：槽位都填满后，**捏合**（不抓字母）即确认拼写
6. **下一题**：答对后捏合或点击「下一题」；答错时正确槽位会锁绿，错误字母弹回
7. **提示**：右下角「💡 翻译」「🔊 读音」支持**手势捏合**或**鼠标点击**

---

## 📁 项目结构

```
├── public/mediapipe/   # MediaPipe Hands 模型与 WASM（需保留）
├── src/
│   ├── App.jsx         # 主界面与游戏逻辑
│   ├── useHandTracking.js  # 摄像头 + 手部追踪
│   ├── words.js        # 四级词汇库（约 1000 词）
│   └── ...
├── index.html
├── vite.config.js
└── package.json
```

---

## 📜 开源协议

MIT
